# Кладётся только если включён ENABLE_LOKI — условие стоит у файла в stack.yaml.
apiVersion: 1

datasources:
  - name: Loki
    uid: dock-loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: false
    jsonData:
      maxLines: 1000
